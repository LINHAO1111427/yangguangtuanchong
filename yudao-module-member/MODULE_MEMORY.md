# 微服务 Memory - member（会员管理模块）

[⬅️ 返回项目总览](../../PROJECT_MEMORY.md)

**依赖模块**：[infra](../yudao-module-infra/MODULE_MEMORY.md)（文件上传、验证码） | **被依赖于**：[content](../yudao-module-content/MODULE_MEMORY.md) • [message](../yudao-module-message/MODULE_MEMORY.md) • [pay](../yudao-module-pay/MODULE_MEMORY.md)

---

## 🔴 强制规则
1. **custom_id必须是7-9位纯数字，避免豹子号和靓号**
2. **用户注册必须发送验证码（手机/邮箱）**
3. **用户资料修改必须验证身份**
4. **关注/粉丝系统必须做防刷保护**
5. **等级积分系统必须有审计日志**

### Implementation Gaps (2025-10-21)
- Custom ID pipeline pending: no `CustomIdGenerator` exists and `MemberUserServiceImpl#createUser` does not assign `custom_id`; design generator + uniqueness guard.
- REST prefix mismatch: app controllers still expose `/member/auth` & `/member/user`; add `/api/v1.0.1/` prefixed aliases to satisfy gateway and front-end.
- Advanced member features (tasks, VIP, visitor system) remain TODO in code; keep them in roadmap and mark status realistically.
## 📋 模块快速理解
- **一句话描述**：管理用户账户、身份认证、关注粉丝、等级积分、任务系统等会员相关功能
- **核心职责**：用户认证 + 个人资料 + 关注系统 + 等级积分 + 任务系统
- **服务端口**：48083
- **数据库**：user_db（用户库）

## 🏗️ 技术架构
### 依赖关系
```
member → infra（发送验证码、文件上传）
member 提供给 → content（用户信息查询）、message（用户信息查询）、pay（用户信息查询）
```

### 项目结构
```
yudao-module-member/
├── yudao-module-member-api/      # API接口定义
│   └── cn.iocoder.yudao.module.member.api/
│       ├── auth/                 # 认证API
│       ├── user/                 # 用户API
│       ├── follow/               # 关注API
│       ├── level/                # 等级API
│       └── task/                 # 任务API
├── yudao-module-member-server/   # 业务实现
│   └── cn.iocoder.yudao.module.member/
│       ├── controller/
│       │   ├── app/              # C端APP接口
│       │   │   ├── auth/         # 认证接口
│       │   │   ├── user/         # 个人资料接口
│       │   │   ├── follow/       # 关注接口
│       │   │   └── task/         # 任务接口
│       ├── service/
│       │   ├── auth/             # 认证服务
│       │   │   ├── AuthService.java              # 认证服务
│       │   │   └── CustomIdGenerator.java        # custom_id生成器
│       │   ├── user/             # 用户服务
│       │   │   └── MemberUserService.java        # 用户服务
│       │   ├── follow/           # 关注服务
│       │   │   ├── FollowService.java            # 关注服务
│       │   │   └── FollowerService.java          # 粉丝服务
│       │   ├── level/            # 等级服务
│       │   │   └── MemberLevelService.java       # 等级积分服务
│       │   └── task/             # 任务服务
│       │       └── MemberTaskService.java        # 任务系统服务
│       ├── dal/
│       │   ├── dataobject/       # 数据对象
│       │   │   ├── MemberUserDO.java             # 用户实体
│       │   │   ├── MemberFollowDO.java           # 关注实体
│       │   │   ├── MemberLevelDO.java            # 等级实体
│       │   │   └── MemberTaskDO.java             # 任务实体
│       │   └── mapper/           # Mapper
│       └── job/
│           ├── LevelUpdateJob.java               # 等级更新定时任务
│           └── TaskRewardJob.java                # 任务奖励发放定时任务
```

## 🚀 已实现功能

### ✅ 用户注册登录（90%）
- **AuthService.java**：认证服务
  - 手机号注册（验证码验证）
  - 邮箱注册（验证码验证）
  - 登录（手机号/邮箱/三方）
  - 登出
  - 刷新token

- **CustomIdGenerator.java**：custom_id生成器
  - 生成7-9位纯数字ID
  - 自动避开豹子号（连续3个及以上相同数字）
  - 自动避开靓号（顺子号/吉祥号等30+个保留号）
  - 支持批量生成和唯一性验证

- **认证接口**：`AppAuthController.java`
  ```
  POST /api/v1.0.1/member/auth/register              # 注册
  POST /api/v1.0.1/member/auth/login                 # 登录
  POST /api/v1.0.1/member/auth/logout                # 登出
  POST /api/v1.0.1/member/auth/refresh-token         # 刷新token
  POST /api/v1.0.1/member/auth/send-verify-code      # 发送验证码
  POST /api/v1.0.1/member/auth/bind-phone            # 绑定手机号
  POST /api/v1.0.1/member/auth/bind-email            # 绑定邮箱
  ```

### ✅ 个人资料（100%）
- **MemberUserService.java**：用户服务
  - 获取个人信息
  - 修改昵称、头像、简介
  - 修改隐私设置
  - 获取用户信息（内部Feign调用）

- **个人资料接口**：`AppUserController.java`
  ```
  GET  /api/v1.0.1/member/user/profile               # 获取个人信息
  PUT  /api/v1.0.1/member/user/update-profile        # 修改个人信息
  GET  /api/v1.0.1/member/user/get-by-id             # 获取用户信息（Feign接口）
  GET  /api/v1.0.1/member/user/get-by-custom-id      # 根据custom_id获取用户
  ```

### ✅ 关注粉丝系统（100%）
- **FollowService.java**：关注服务
  - 关注用户
  - 取消关注
  - 关注列表
  - 检查是否关注

- **FollowerService.java**：粉丝服务
  - 粉丝列表
  - 粉丝数统计
  - 共同粉丝

- **关注接口**：`AppFollowController.java`
  ```
  POST /api/v1.0.1/member/follow/follow              # 关注用户
  POST /api/v1.0.1/member/follow/unfollow            # 取消关注
  GET  /api/v1.0.1/member/follow/following-list      # 关注列表
  GET  /api/v1.0.1/member/follow/followers-list      # 粉丝列表
  GET  /api/v1.0.1/member/follow/is-following        # 检查是否关注
  GET  /api/v1.0.1/member/follow/common-followers    # 共同粉丝
  ```

### ✅ 等级积分系统（100%）
- **MemberLevelService.java**：等级积分服务
  - 获取用户等级
  - 增加/减少积分
  - 积分兑换
  - 等级任务列表

- **等级接口**：`AppLevelController.java`
  ```
  GET  /api/v1.0.1/member/level/my-level             # 获取我的等级
  GET  /api/v1.0.1/member/level/user-level           # 获取用户等级
  POST /api/v1.0.1/member/level/add-points           # 增加积分
  POST /api/v1.0.1/member/level/exchange-points      # 兑换商品
  GET  /api/v1.0.1/member/level/my-tasks             # 等级任务列表
  ```

### ⚠️ 待开发功能

#### 访客系统与VIP权限（0%）- P1优先级 ✅ 新增
**需求**（来自需求文档.md 第83-84行）：
- 用户可查看谁看过自己（访客列表）
- 普通用户需要通过看广告获得5小时临时VIP权限，才能查看访客
- VIP权限到期后需重新看广告获取
- 后期支持永久VIP会员功能

**流程**：
1. 用户浏览他人主页 → 记录访客信息
2. 访客用户进入"我的"页面 → 显示"查看谁看过我"按钮
3. 点击按钮 → 提示需要VIP权限，显示"看广告获得5小时VIP"按钮
4. 看完广告 → 系统颁发5小时临时VIP TOKEN
5. 用户可查看访客列表（排序按访问时间）

**表设计**：
- `member_visitor`（访客记录表）
  - visitor_id、viewer_id、viewed_user_id、visit_time, device_info
  - 索引：idx_viewed_user_id, idx_visit_time
  - 分区：按月分区（保留1年）

- `member_vip_privilege`（VIP权限表）
  - privilege_id、user_id、privilege_type（0临时/1永久）、expire_time, create_time
  - 索引：idx_user_id, idx_expire_time
  - 备注：用于快速检查用户是否有有效VIP权限

**关键实现**：
- 访客记录采用异步处理（通过Kafka异步记录，不影响页面加载）
- VIP权限检查在Redis中存储（加快查询速度）
- 临时VIP自动过期清理（定时任务每小时执行一次）

**开发计划**：3人天
- Day1: 设计访客数据模型、权限模型，实现访客记录拦截器
- Day2: 实现查看访客列表接口、VIP权限管理
- Day3: 实现看广告获取临时VIP流程、权限过期清理

#### 名片守护榜（0%）- P1优先级 ✅ 新增
**需求**（来自需求文档.md 第100-102行）：
- 粉丝可充值支持用户（520.99元档位）
- 充值后在用户的"名片守护榜"中显示粉丝名单
- 显示周守护榜单（每周更新）
- 充值金额进入平台公对公账户

**功能说明**：
- 用户可在"我的"页面查看周守护榜单
- 显示本周给自己充值的粉丝名单（按充值金额排序）
- 每周一凌晨自动更新为新的一周
- 粉丝可在用户详情页点击"守护"按钮进行充值

**表设计**：
- `member_guardian`（守护记录表）
  - guardian_id、user_id、supporter_id、amount, guardian_type（520), create_time
  - 索引：idx_user_id, idx_create_time
- `member_guardian_weekly_rank`（周守护排行表）
  - rank_id、user_id、week_start_date, supporter_list(JSON), total_amount
  - 索引：idx_user_id, idx_week_start_date

**开发计划**：2人天

#### 实名认证完整版（0%）- P2优先级 ✅ 补充完整
**需求**（来自需求文档.md 第98行备注）：
- 身份证OCR识别
- 人脸刷脸验证（活体检测）
- 实名认证审核
- 认证通过后获得"认证标签"展示

**实现方案**：
需要集成第三方服务（推荐阿里云或腾讯云）：
1. 身份证识别：阿里云OCR服务
2. 人脸识别：腾讯云人脸识别+活体检测
3. 实名认证审核：后台人工或自动审核

**流程**：
1. 用户点击"实名认证"
2. 上传身份证照片 → OCR识别提取信息
3. 进行人脸刷脸验证 → 活体检测
4. 后台审核（人工或自动）
5. 审核通过 → 用户获得"实名认证"标签
6. 在个人主页和搜索结果中显示认证标签

**表设计**：
- `member_certification`（实名认证表）
  - cert_id、user_id、id_card_number, id_card_name, cert_status（0审核中/1通过/2拒绝）
  - real_name, face_verification_result, cert_time

**开发计划**：3人天
- Day1: 集成第三方SDK，实现身份证OCR
- Day2: 实现人脸识别和活体检测
- Day3: 实现认证审核流程和标签展示

#### 任务系统（0%）- P0优先级
**需求**：用户完成任务获得奖励
- 任务类型：邀请好友、分享作品、每日登录、评论作品等
- 任务奖励：金币、积分、代币券等
- 任务进度：显示完成进度
- 任务领取：点击领取奖励

**表设计**：
- `member_task`（任务表）
  - task_id、name、description、task_type、reward_type、reward_amount
- `member_task_progress`（任务进度表）
  - user_id、task_id、progress_value、status（0未开始/1进行中/2已完成/3已领取）
- `member_task_reward_record`（奖励记录表）
  - user_id、task_id、reward_type、reward_amount、create_time

**开发计划**：3人天
- Day1: 设计任务系统数据模型
- Day2: 实现任务进度计算和奖励规则
- Day3: 开发任务相关接口，集成到积分系统

#### 实名认证（0%）- P2优先级
**需求**：用户实名认证功能
- 身份证信息提交
- 人脸识别验证
- 认证审核
- 认证状态管理

**开发计划**：3人天

#### 隐私设置（40%）- P2优先级
**需求**：用户隐私设置
- 谁可以看我的资料
- 谁可以给我发消息
- 谁可以关注我

**开发计划**：1人天

#### 团队权限认证（0%）- P1优先级
**需求**：支持团队账户和权限管理
- 团队账户创建
- 成员管理
- 权限分配
- 财务分账

**开发计划**：3人天

## 🔧 核心代码位置

### Controller（接口层）
- `AppAuthController.java` - 认证接口 - `/api/v1.0.1/member/auth/*`
- `AppUserController.java` - 个人资料接口 - `/api/v1.0.1/member/user/*`
- `AppFollowController.java` - 关注接口 - `/api/v1.0.1/member/follow/*`
- `AppLevelController.java` - 等级接口 - `/api/v1.0.1/member/level/*`
- `AppTaskController.java` - 任务接口 - `/api/v1.0.1/member/task/*`

### Service（业务层）
- `AuthService.java` - 认证服务
- `CustomIdGenerator.java` - custom_id生成器
- `MemberUserService.java` - 用户服务
- `FollowService.java` - 关注服务
- `FollowerService.java` - 粉丝服务
- `MemberLevelService.java` - 等级服务
- `MemberTaskService.java` - 任务服务

### DataObject（数据对象）
- `MemberUserDO.java` - 用户实体
- `MemberFollowDO.java` - 关注实体
- `MemberLevelDO.java` - 等级实体
- `MemberTaskDO.java` - 任务实体

## ⚡ 性能优化记录
- Redis缓存用户信息（1小时TTL）
- Redis缓存关注关系（30分钟TTL）
- 等级更新定时任务每天凌晨2点执行
- custom_id生成使用本地缓存避免重复查询

## ⚠️ 注意事项

### 开发规范
1. **custom_id生成必须验证唯一性**
2. **关注操作必须做防刷保护（1秒内只能操作1次）**
3. **积分增减必须记录审计日志**
4. **任务完成必须有后端验证**

### 常见坑点
1. custom_id避开的靓号要定期更新
2. 关注关系的双向维护（follow和follower表）
3. 等级提升要考虑积分回滚的影响
4. 任务完成的验证要防止刷单

## 📊 数据库表设计

### 核心表
1. **member_user**（用户表）
   - 主键：id
   - 字段：custom_id（7-9位唯一）, phone, email, password_hash, nickname, avatar, intro, real_name, id_card
   - 索引：uk_custom_id, uk_phone, uk_email, idx_create_time
   - 分区：按用户库分片

2. **member_follow**（关注表）
   - 主键：id
   - 字段：follower_id, following_id, create_time
   - 唯一索引：uk_follower_following（follower_id + following_id）
   - 索引：idx_follower_id, idx_following_id

3. **member_level**（等级表）
   - 主键：id
   - 字段：user_id, level（1-10）, points, total_points, create_time, update_time
   - 索引：uk_user_id, idx_level

4. **member_task**（任务表）
   - 主键：id
   - 字段：task_name, task_type, reward_type, reward_amount, description
   - 索引：idx_task_type

5. **member_task_progress**（任务进度表）
   - 主键：id
   - 字段：user_id, task_id, progress_value, status（0/1/2/3）, create_time, update_time
   - 唯一索引：uk_user_task（user_id + task_id）
   - 索引：idx_user_id, idx_status

## 🔄 更新记录
- 2025-10-09：custom_id生成机制完成
- 2025-10-09：基本注册登录功能完成
- 2025-10-09：关注粉丝系统完成
- 2025-10-09：等级积分系统完成
- 2025-10-16：初始创建MODULE_MEMORY
- 2025-10-16：**✅ 重大补充** - 添加访客系统、VIP权限、名片守护榜、实名认证完整说明
## ⏳ 待开发功能（前端对接需要）

### 🔴 P0 - 用户信息API（1人天）✅ 前端急需
**需求来源**: 前端channel/mine.vue需要显示用户信息和作品
**API**: GET /api/v1.0.1/user/info, /user/works, /user/stats
**数据表**: 扩展member_user表，添加works_count/view_count/like_count字段

### 🟡 P1 - 用户频道管理API（1人天）✅ 前端需要
**需求来源**: 前端index/select.vue需要频道订阅功能
**API**: GET/POST /api/v1.0.1/user/channels
**数据表**: member_user_channel（用户频道订阅表）

详细设计见 API_STATUS.md
